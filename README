Author batuatalay

-> for Launching server

sudo apt update
sudo apt install nodejs
sudo apt install npm
sudo npm install pm2
sudo apt install git

->for Launching script
pm2 start server.js

OR 

https://docs.docker.com/engine/install/ubuntu/
sudo usermod -aG docker ubuntu
docker container run -d -p 80:3000 --name batlight -v /home/ubuntu/public:/batlight/public batuatalay/batlight

docker container run -d -p 80:80 --name nginxServer -v /home/ubuntu/public:/usr/share/nginx/html nginx

pm2 status
