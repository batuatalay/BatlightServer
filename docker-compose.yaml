version: '3.8'
services:
  batlight:
    container_name: batlight
    build: .
    working_dir: /app
    ports:
      - "80:3000"
      - "3000:3000"
    networks:
      - serverNet
    restart: always

networks:
  serverNet:
    driver: bridge"